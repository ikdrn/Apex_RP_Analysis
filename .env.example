# Supabase (required)
SUPABASE_URL=https://mcpmqwaybokvxkowwmor.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# CORS (optional) — defaults to "*" when unset
ALLOWED_ORIGIN=https://apex-rp-analysis.vercel.app

# Basic Auth (optional) — enforced only when both are set
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
